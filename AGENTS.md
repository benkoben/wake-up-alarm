# Missing dependencies

The datetime module is not installed by default with the micropython firmware.

You can download packages using 

```
 mpremote mip install <package>
```

Example

```
 mpremote mip install datetime
```